package app.validators;
import app.dto.PersonDTO;

public class PersonValidator {
    public static boolean validatePerson(PersonDTO personDTO) {
        return true; // Suppose validation always succeeds for now
    }
}
