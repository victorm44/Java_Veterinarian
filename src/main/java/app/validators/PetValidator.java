package app.validators;
import app.dto.PetDTO;

public class PetValidator {
    public static boolean validatePet(PetDTO petDTO) {
        return true; // Supongamos que la validación siempre es exitosa por ahora
    }
}
